********** DATA MINING FINAL PROJECT -  **************

-------------------------------------------------------------------------------------
The Programs uses the following built-in packages

1.copy
2.warnings

It also uses the following external package:

1.numpy
2.pandas
3.matplotlib
4.sklearn

If the above external package is not installed in the pc, one can run the following commands in the command prompt:

pip install numpy
pip install pandas
pip install matplotlib
pip install sklearn
-------------------------------------------------------------------------------------

To run the program: Open command prompt at the folder where the files are present or open the command prompt and change working directory to that folder.

1. Type the command in the following format in cmd:
	
HousePred.py
  
	// The program returns the text as well as plots showing the results. 

The End.
*************************************************************************************